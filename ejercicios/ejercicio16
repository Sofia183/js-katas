const placesToTravel = ['Japon', 'Venecia', 'Murcia', 'Santander', 'Filipinas', 'Madagascar'];

// Usamos el bucle for...of para recorrer cada destino en el array
for (const place of placesToTravel) {
  // Imprimimos el valor de cada destino
  console.log(place);
}
